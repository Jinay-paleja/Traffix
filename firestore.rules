rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This app writes to Firestore via Firebase Admin SDK on the backend.
    // Keep client-side direct access blocked by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
